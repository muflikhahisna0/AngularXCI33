app.controller("HomeController", [
    "$scope",
    "$location",
    "$timeout",
    "VoliServices",
    "sharedData",
    function ($scope, $location, $timeout, VoliServices, sharedData) {
        var self = $scope;

        self.init = function () { };

        self.init();
    },
]);
